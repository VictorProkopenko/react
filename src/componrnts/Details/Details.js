const Details = ({user}) => {

    //const {id,name}= user;
    console.log({user});
    return (
        <div>
            {/*{id}{name}*/}
            {/*{user.id} {user.name}*/}
        </div>
    );
};

export default Details;